{
  "name": "sendkit-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "format": "oxfmt . --write",
    "format:check": "oxfmt . --check",
    "lint": "oxlint . --deny-warnings",
    "lint:fix": "oxlint . --fix",
    "build": "bun run build:core && bun run build:cli && bun run build:local-mcp",
    "build:core": "bun run --filter @rishi1006/sendkit-core build",
    "build:cli": "bun run --filter @rishi1006/sendkit build",
    "build:local-mcp": "bun run --filter @rishi1006/sendkit-mcp build",
    "dev:cli": "bun run packages/cli/src/index.ts",
    "dev:local-mcp": "bun run packages/local-mcp/src/index.ts",
    "dev:remote-mcp": "bun run apps/remote-mcp/api/index.ts",
    "release:pack:core": "bun run --filter @rishi1006/sendkit-core pack:dry",
    "release:pack:cli": "bun run --filter @rishi1006/sendkit pack:dry",
    "release:pack:local-mcp": "bun run --filter @rishi1006/sendkit-mcp pack:dry",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "oxfmt": "^0.53.0",
    "oxlint": "^1.68.0",
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3"
  }
}
